# R session / scratch output that should not be tracked
Rplots.pdf
.Rhistory
.RData
.Rproj.user/

# One-off audit/check dumps generated while investigating the pipeline
# (note: neha_proteomics_audit_fast.txt is already tracked, so it is deliberately not
#  listed here -- ignoring a tracked file has no effect and would be misleading)
canonical_gsea_implementation_check.txt

# Demo outputs are regenerated by run_demo.R
demo/output/
